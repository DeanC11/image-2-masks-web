import { Text, Title } from "@mantine/core";

const AppTitle = () => {
  return (
    <div>
      <Title order={2}>Image 2 Masks</Title>
      <Text size="sm">Create Assembly masks from images</Text>
    </div>
  );
};

export default AppTitle;
